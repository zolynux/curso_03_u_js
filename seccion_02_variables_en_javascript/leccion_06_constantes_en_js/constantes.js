/*
Contantes en JavaScript

Una constante es una variable que una vez definido 
su valor, ya no se puede modificar

Como buena práctica, el nombre de una constante se 
define en mayúsculas,  si tiene más palabras se 
separan con guión bajo (_).

Ej. const MI_CONSTANTE = 10;
  o
    const SEGUNDOS_POR_MINUTO = 60;
*/

// Constantes
const MI_CONSTANTE = 10;
// MI_CONSTANTE = 20; // No podemos cambiar el valor de una constante

console.log(MI_CONSTANTE);

console.log(Math.PI);

// Constante segundos por minuto
const SEGUNDOS_POR_MINUTO = 60;
console.log(SEGUNDOS_POR_MINUTO);